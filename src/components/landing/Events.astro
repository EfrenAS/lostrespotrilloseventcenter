---
import type { ImageMetadata } from "astro";
import { Picture } from "astro:assets";
import { getCollection } from "astro:content";
import Decorator from "../../components/Decorator.astro";

const events = await getCollection("events");
---

<section class="events">
  <h2 class="subtitle">
    Events
    <span class="subtitle-esp">Eventos</span>
  </h2>
  <p class="paragraph">
    Bring all the joy of your event to our venue and let's celebrate together,
    it can be any of these events, or whatever you need.
  </p>
  <div class="events__container">
    {
      events.map((event) => (
        <div class="event">
          <div class="event__image">
            <Picture
              src={event.data.heroImage}
              alt={event.data.imageAttr.alt}
              width={event.data.imageAttr.width}
              height={event.data.imageAttr.height}
            />
          </div>
          <div class="event__content">
            <h3 class="event__title">{event.data.title}</h3>
            <Decorator />
            <div class="event__text">
              <p>
                Lorem ipsum dolor sit amet, consectetur adipisicing elit.
                Voluptate reiciendis corrupti officiis suscipit. Facilis
                adipisci, autem saepe quidem impedit iure, perferendis,
                molestiae odit aperiam quos deleniti amet cupiditate consectetur
                quia labore ad similique reiciendis dolor eligendi consequuntur
                porro ex. A doloribus quae, autem rerum dolorum repellat dolore
                perspiciatis quas culpa!
              </p>
              <slot />
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>
